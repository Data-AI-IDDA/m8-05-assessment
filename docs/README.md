# docs

Drop `screenshot.png` here after running the app (`streamlit run app.py`).

Suggested shot: the chat thread mid-conversation (a study question + streaming
answer) with the sidebar visible showing the temperature slider, the "Clear
chat" button, and the live input/output token counters.
